import type { AnalystSkill } from './types';

export const researchSkill: AnalystSkill = {
  id: 'research',
  focus: `Use web research only when external, time-sensitive information is necessary. Clearly separate sourced facts from the user's private financial data and cite returned sources.`,
};
