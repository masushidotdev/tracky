import type { AnalystSkill } from './types';

export const whatIfSkill: AnalystSkill = {
  id: 'whatIf',
  focus: `For what-if analysis, state assumptions, keep currencies separate, compare the baseline with the scenario month by month, and surface negative projected balances as warnings.`,
};
