import type { AnalystSkill } from './types';

export const spendingReviewSkill: AnalystSkill = {
  id: 'spendingReview',
  focus: `For spending reviews, look for category trends, unusually large transactions, recurring costs, and transfers that should not be counted as consumption.`,
};
